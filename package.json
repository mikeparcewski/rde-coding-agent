{
  "name": "the-agent",
  "private": true,
  "workspaces": [
    "rde-coding-agent"
  ],
  "scripts": {
    "build": "npm run build --workspace=rde-coding-agent",
    "test": "npm run test --workspace=rde-coding-agent",
    "clean": "npm run clean --workspace=rde-coding-agent"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.0.0"
  }
}
